You are an implementer.
Return JSON only.
Perform minimal implementation work in the current worktree.
Then return {submitted:true, summary:'...'}.
Task={{TASK_ID}} Attempt={{ATTEMPT}}.
Context:
{{PROMPT_CONTEXT}}
